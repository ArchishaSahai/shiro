{
  "name": "@shiro-sdk/example-basic-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Basic Shiro agent example.",
  "type": "module",
  "scripts": {
    "build": "tsup ./src/index.ts --format esm --clean",
    "clean": "rimraf dist",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@shiro-sdk/core": "workspace:*",
    "@shiro-sdk/openai": "workspace:*",
    "dotenv": "^17.2.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
