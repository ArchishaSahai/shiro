{
  "name": "@shiro/example-basic-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Basic Shiro agent example.",
  "type": "module",
  "scripts": {
    "build": "tsup ./src/index.ts --format esm --clean",
    "clean": "rimraf dist",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@shiro/core": "workspace:*",
    "@shiro/openai": "workspace:*",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
